// Keep in sync with `basePath` in next.config.ts. Next.js auto-prefixes
// <Link> and router.push(), but not raw fetch()/<a> calls, so those need this.
export const BASE_PATH = "/product/planning-poker";
